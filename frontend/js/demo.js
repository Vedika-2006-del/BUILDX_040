window.runRohanDemo=function(){let steps=['Emergency Created','Hospital Matching','Hospital Accepted','Resources Reserved','Blood Reserved','Ambulance Assigned','En Route','Patient Arrived','Admitted'];const box=document.getElementById('trackTimeline');if(!box)return;let i=0;const tick=()=>{const done=i+1;box.innerHTML=steps.map((s,k)=>`<div class="big-step ${k<done?'done':''}"><span>${k<done?'✓':k+1}</span><b>${s}</b><small>${k<done?'Completed':'Pending'}</small></div>`).join('');const eta=document.getElementById('trackEta');if(eta)eta.textContent=Math.max(0,13-i*2)+' min';MediResQ.toast(i<8?steps[i]+' • simulated event':'Rohan admitted • Demo workflow complete');i++;if(i<steps.length)setTimeout(tick,1000)};tick()};